using UnityEngine;

namespace BlockSort.GameUI
{
    public class Menu : MonoBehaviour
    {
        private void Start()
        {
            //Button btn = transform.Find("MenuCancel").gameObject.GetComponent<Button>();
            //btn.onClick.AddListener(CancelButtonTaskOnClick);
        }

        private void CancelButtonTaskOnClick()
        {
            //this.gameObject.SetActive(false);
        }
    }
}
